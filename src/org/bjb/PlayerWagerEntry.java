package org.bjb;import java.awt.*;  import java.awt.event.*;  import javax.swing.*;     class PlayerWagerEntry extends JDialog{  //	final static JLabel ico = new JLabel(UIManager.getIcon("OptionPane.questionIcon"));	final static JLabel ico = new JLabel(Card.getIcon("images/rocket-icon-64x64.png")); 	// Create an action    Action canact = new AbstractAction("Cancel") {        // This method is called when the button is pressed        public void actionPerformed(ActionEvent evt) {            setVisible(false);            dispose();        }    };	JButton cancel = new JButton(canact);		// Create an action    Action okact = new AbstractAction("OK") {        // This method is called when the button is pressed        public void actionPerformed(ActionEvent evt) {            save();            setVisible(false);        	dispose();        }    }; 	JButton ok = new JButton(okact); 	Player p = null;	JTextField wager = new JTextField(7); 	int value = 0; 	private static final Color back = Color.yellow; 	 	public PlayerWagerEntry(Player p) { 		super(new JFrame(""),true); 		this.p = p;			setTitle("Wager Entry");		setUndecorated(true);		getContentPane().setBackground(back);		getContentPane().setLayout(new GridBagLayout());		GridBagConstraints c = new GridBagConstraints();		c.gridx = 0;		c.gridy = 0;		getContentPane().add(ico,c);		c.gridx = 1;		JLabel lbl = new JLabel("<html>Enter <font color=green>Wager</html>");		lbl.setBackground(Color.white);		getContentPane().add(lbl,c);		c.gridx = 2;		wager.setBackground(Color.white);		wager.setText(new Integer(p.getBet()).toString());		wager.selectAll();		getContentPane().add(wager,c);		c.gridx = 0;		c.gridy = 1;		c.gridwidth = 2;		JPanel buttons = new JPanel();		buttons.setBackground(back);		cancel.setBackground(back);		buttons.add(cancel);		ok.setBackground(back);		buttons.add(ok);		getRootPane().setDefaultButton(ok);		getContentPane().add(buttons,c);		pack();		Dimension tmp = Toolkit.getDefaultToolkit().getScreenSize();		int x = p.getX();		if (x + getWidth()> (int)tmp.getWidth()) x = (int)tmp.getWidth() - getWidth() - 5; 		setLocation(x,p.getY()+75);	}		private void save() {		try {			value = new Integer(wager.getText()).intValue(); 		}		catch (Throwable tossed) {			value = p.getBet();		}	}		public int getWager() { return value; }}  