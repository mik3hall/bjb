package org.bjb;import java.awt.*;  import java.awt.event.*;  import javax.swing.*;     class PlayerInsuranceCheck extends JDialog{  	boolean insurance = false;	 	// Create an action    Action canact = new AbstractAction("Cancel") {        // This method is called when the button is pressed        public void actionPerformed(ActionEvent evt) {            setVisible(false);            dispose();        }    };	JButton cancel = new JButton(canact);		// Create an action    Action okact = new AbstractAction("OK") {    	static final private long serialVersionUID = -1L;        // This method is called when the button is pressed        public void actionPerformed(ActionEvent evt) {            insurance = true;            setVisible(false);        	dispose();        }    }; 	JButton ok = new JButton(okact); 	Player p = null;	JTextField wager = new JTextField(7); 	int value = 0; 	Color back = Color.yellow; 	 	public PlayerInsuranceCheck(Player p) { 		super(new JFrame("")); 		this.p = p;			setModal(true);		setTitle("Insurance?");		setLocation(p.getX(),p.getY()+75);		setUndecorated(true);		getContentPane().setBackground(back);		getContentPane().setLayout(new GridBagLayout());		GridBagConstraints c = new GridBagConstraints();		c.gridx = 0;		c.gridy = 0;		JLabel lbl = new JLabel("<html>Enter <font color=green>Wager</html>");		lbl.setBackground(Color.white);		getContentPane().add(lbl,c);		c.gridx = 1;		wager.setBackground(Color.white);		wager.setText(new Integer(p.getBet()).toString());		wager.selectAll();		getContentPane().add(wager,c);		c.gridx = 0;		c.gridy = 1;		c.gridwidth = 2;		JPanel buttons = new JPanel();		buttons.setBackground(back);		cancel.setBackground(back);		buttons.add(cancel);		ok.setBackground(back);		buttons.add(ok);		getRootPane().setDefaultButton(ok);		getContentPane().add(buttons,c);		pack();	} /*	private void save() {		try {			value = new Integer(wager.getText()).intValue(); 		}		catch (Throwable tossed) {			value = p.getBet();		}	}*/		public int getWager() { return value; }}  